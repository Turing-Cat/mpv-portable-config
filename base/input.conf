# Shared mpv key bindings.
# Keep this file small so removing a binding restores mpv defaults.

# Left / middle mouse button: pause or resume.
MBTN_LEFT cycle pause
MBTN_MID  cycle pause

# Lowercase t: toggle always-on-top. mpv defaults to Shift+T.
t cycle ontop
